<div class="comments">
    {{#each this}}
    <div class="mt-3 comment">
        <div class="divider">
            <p class="m-0 font-weight-bold main-heading">{{user.username}}</p>
            <p class="m-0 mb-1 font-italic sub-txt">{{formatDate created_at}}</p>
        </div>
        <div class="">
            <p class="m-0 mt-1 main-heading">{{comment_text}}</p>
        </div>
    </div>
    {{/each}}
</div>